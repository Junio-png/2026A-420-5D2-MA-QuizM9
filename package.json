{
  "name": "quiz-m9",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "verifier": "node server/verifier/verifier.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
