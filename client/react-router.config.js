/**
 * React Router en mode framework. ssr: true — chaque page arrive du serveur
 * déjà rendue en HTML, puis React s'hydrate dans le navigateur.
 */
export default {
  appDirectory: 'app',
  ssr: true,
};
